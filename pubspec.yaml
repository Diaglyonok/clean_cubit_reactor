name: clean_cubit_reactor
description: Lightweight coordination layer for Cubit with automatic synchronization between multiple cubits using Reactor.
version: 1.0.3
repository: https://github.com/Diaglyonok/clean_cubit_reactor
issue_tracker: https://github.com/Diaglyonok/clean_cubit_reactor/issues
homepage: https://github.com/Diaglyonok/clean_cubit_reactor

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
